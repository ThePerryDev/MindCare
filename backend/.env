# App
NODE_ENV=development
PORT=3000

# Database
MONGO_URI=mongodb://localhost:27017/mindcare

# CORS (opcional; use * em dev)
CORS_ORIGIN=*

# Fuso horário padrão (opcional, usado no onboarding)
DEFAULT_TZ=America/Sao_Paulo

# Segurança (reserva para futuramente: tokens, JWT, etc.)
JWT_SECRET=troque-esta-chave-em-producao
