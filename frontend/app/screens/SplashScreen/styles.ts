import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flexGrow: 1,
    justifyContent: 'center',
  },
  image: {
    alignSelf: 'center',
    height: 250,
    resizeMode: 'contain',
    width: 250,
  },
});
